import { Database, KeyRound, Network, RefreshCw, ShieldCheck } from "lucide-react";
import Link from "next/link";
import { PageHeader } from "@/components/ui";
import { SettingsForm } from "@/components/settings-form";
import { getQuotaPolicy, getStatusThresholds } from "@/server/settings";
import { env } from "@/lib/env";
export const metadata={title:"Settings"};
export default async function SettingsPage(){const [thresholds,quota]=await Promise.all([getStatusThresholds(),getQuotaPolicy()]);return <><PageHeader title="Settings" description="Presence thresholds, quota reset boundaries, WireGuard IPAM, and deployment-controlled security posture."/><div className="grid-sidebar"><section className="card"><div className="card-header"><h2>Presence and quota policy</h2></div><div className="card-body"><SettingsForm online={thresholds.onlineSeconds} recent={thresholds.recentSeconds} timezone={quota.timezone} weekStartsOn={quota.weekStartsOn} monthlyResetDay={quota.monthlyResetDay}/></div></section><aside className="card"><div className="card-header"><h2>Runtime posture</h2></div><div className="card-body"><dl className="detail-list"><dt><RefreshCw size={15}/> Full synchronization</dt><dd>Every {env().SYNC_INTERVAL_SECONDS} seconds</dd><dt><RefreshCw size={15}/> Statistics polling</dt><dd>Approximately every {env().MIKROTIK_STATS_INTERVAL} seconds</dd><dt><ShieldCheck size={15}/> Session lifetime</dt><dd>{env().SESSION_TTL_HOURS} hours</dd><dt><KeyRound size={15}/> Secret encryption</dt><dd>AES-256-GCM</dd><dt><Database size={15}/> Database</dt><dd>PostgreSQL</dd></dl><p className="cell-sub">Polling-based quotas can exceed their limit by traffic transferred between polls. Security-sensitive runtime values are controlled through environment variables and container restart.</p></div></aside></div><section className="card" style={{marginTop:16}}><div className="card-header"><h2><Network size={17}/> WireGuard Pools</h2><Link className="button button-primary" href="/pools">Manage pools</Link></div><div className="card-body"><p>Configure application-side static client address pools, reserved addresses, utilization, and safe automatic allocation. These are not MikroTik DHCP pools.</p></div></section></>}
