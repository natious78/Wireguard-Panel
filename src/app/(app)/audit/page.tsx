import { BookOpenCheck, Filter, Search } from "lucide-react";
import { query } from "@/lib/db";
import { EmptyState, PageHeader, StatusBadge } from "@/components/ui";

type Row={id:string;created_at:Date;username:string|null;action:string;result:string;router_name:string|null;peer_name:string|null;details:Record<string,unknown>;ip_address:string|null;total_count:string};
export const metadata={title:"Audit Logs"};
export default async function AuditPage({searchParams}:{searchParams:Promise<{q?:string;result?:string;page?:string}>}){const p=await searchParams;const q=p.q||"";const resultFilter=p.result||"";const page=Math.max(1,Number(p.page)||1);const limit=75;const result=await query<Row>(`SELECT a.*,r.name router_name,p.name peer_name,count(*) OVER()::text total_count FROM audit_logs a LEFT JOIN routers r ON r.id=a.router_id LEFT JOIN peers p ON p.id=a.peer_id WHERE ($1='' OR a.action ILIKE '%'||$1||'%' OR coalesce(a.username,'') ILIKE '%'||$1||'%' OR coalesce(r.name,'') ILIKE '%'||$1||'%' OR coalesce(p.name,'') ILIKE '%'||$1||'%') AND ($2='' OR a.result=$2) ORDER BY a.created_at DESC LIMIT $3 OFFSET $4`,[q,resultFilter,limit,(page-1)*limit]);const total=Number(result.rows[0]?.total_count||0);return <><PageHeader title="Audit logs" description="Administrative and system actions without credentials, private keys, or encryption material."/><section className="card"><form className="table-toolbar"><div className="search"><Search/><input name="q" defaultValue={q} placeholder="Search action, user, router, peer…" aria-label="Search audit logs"/></div><select name="result" defaultValue={resultFilter} aria-label="Filter audit result"><option value="">All results</option><option value="success">Success</option><option value="warning">Warning</option><option value="failure">Failure</option></select><button className="button button-small"><Filter/>Apply</button></form>{result.rows.length?<div className="table-wrap"><table><thead><tr><th>Timestamp</th><th>User</th><th>Action</th><th>Target</th><th>Result</th><th>Source IP</th></tr></thead><tbody>{result.rows.map(a=><tr key={a.id}><td>{new Date(a.created_at).toLocaleString()}</td><td>{a.username||"system"}</td><td className="cell-main">{a.action.replaceAll("_"," ")}</td><td><div>{a.peer_name||a.router_name||"Application"}</div>{a.peer_name&&a.router_name&&<div className="cell-sub">{a.router_name}</div>}</td><td><StatusBadge status={a.result}/></td><td className="mono">{a.ip_address||"—"}</td></tr>)}</tbody></table></div>:<EmptyState icon={BookOpenCheck} title="No audit events" message="Administrative and synchronization activity will be recorded here."/>}</section>{total>limit&&<div className="actions" style={{justifyContent:"flex-end",marginTop:12}}><span className="cell-sub">{total} events</span>{page>1&&<a className="button button-small" href={`/audit?q=${encodeURIComponent(q)}&result=${resultFilter}&page=${page-1}`}>Previous</a>}{page*limit<total&&<a className="button button-small" href={`/audit?q=${encodeURIComponent(q)}&result=${resultFilter}&page=${page+1}`}>Next</a>}</div>}</>}
