import{NextRequest}from"next/server";import{z}from"zod";import{audit}from"@/lib/audit";import{fail,handleApiError,ok}from"@/lib/api";import{requireUser}from"@/lib/auth";import{validateCsrf}from"@/lib/csrf";import{createPeerProfile}from"@/server/policy-service";
const schema=z.object({name:z.string().trim().min(2).max(100),description:z.string().max(500).nullable().optional(),poolId:z.string().uuid().nullable().optional(),dns:z.string().max(255).nullable().optional(),clientAllowedIps:z.string().max(500).nullable().optional(),mtu:z.coerce.number().int().min(576).max(9000).nullable().optional(),keepalive:z.coerce.number().int().min(0).max(65535).nullable().optional(),quotaGb:z.coerce.number().positive().nullable().optional(),quotaPeriod:z.enum(["one_time","daily","weekly","monthly"]).nullable().optional(),bandwidthProfileId:z.string().uuid().nullable().optional(),expirationDays:z.coerce.number().int().positive().nullable().optional(),enabled:z.boolean().default(true)});
export async function POST(request:NextRequest){const auth=await requireUser("profile:manage");if(!auth.user)return fail(auth.error,auth.status);if(!(await validateCsrf(request)))return fail("Security token expired.",403);try{const input=schema.parse(await request.json());const id=await createPeerProfile({...input,quotaBytes:input.quotaGb?BigInt(Math.round(input.quotaGb*1e9)):null},auth.user.id);await audit({user:auth.user,action:"peer_profile_created",result:"success",details:{id,name:input.name}});return ok({id},{status:201})}catch(error){return handleApiError(error)}}
